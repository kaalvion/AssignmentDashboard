{
  "name": "assignment-deadline-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend 2>/dev/null || true; npm install && npm run build && cp -r dist ../ 2>/dev/null || true"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "sqlite3": "^6.0.1"
  }
}
